@extends('layouts.dashboard')
@section('content')
<div class="container-fluid py-3"><div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4"><div><h1 class="h3 mb-1">Формы обучения</h1><p class="text-muted mb-0">Форма обучения используется при регистрации и зачислении ученика.</p></div><a class="btn btn-primary" href="{{ route('dashboard.academic.enrollment-modes.create') }}">Добавить форму обучения</a></div>
<div class="card border-0 shadow-sm"><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th>Название</th><th>Краткое название</th><th>Код</th><th>Порядок</th><th>Статус</th><th>Действия</th></tr></thead><tbody>@forelse($modes as $mode)<tr><td><strong>{{ $mode->name_ru }}</strong>@if($mode->description)<div class="small text-muted">{{ $mode->description }}</div>@endif</td><td>{{ $mode->short_name_ru ?: '—' }}</td><td><code>{{ $mode->code }}</code></td><td>{{ $mode->display_order }}</td><td><span class="badge {{ $mode->is_active?'bg-success':'bg-secondary' }}">{{ $mode->is_active?'Активна':'Неактивна' }}</span>@if($mode->enrollments_count)<div class="small text-muted mt-1">Зачислений: {{ $mode->enrollments_count }}</div>@endif</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('dashboard.academic.enrollment-modes.edit',$mode) }}">Изменить</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">Формы обучения не настроены.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
