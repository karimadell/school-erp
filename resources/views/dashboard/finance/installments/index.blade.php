@extends('layouts.dashboard')
@section('content')<div class="container-fluid py-4"><h1 class="h3">Контроль рассрочки</h1><div class="row g-3 mb-4">@foreach(['Предстоящие'=>fn($i)=>$i->derivedStatus()==='future','Просроченные'=>fn($i)=>$i->derivedStatus()==='overdue','Собранные'=>fn($i)=>$i->derivedStatus()==='paid','К оплате'=>fn($i)=>$i->derivedStatus()!=='paid'] as $label=>$filter)<div class="col-6 col-lg-3"><div class="card card-body"><span class="text-muted">{{ $label }}</span><strong>{{ $installments->filter($filter)->count() }}</strong></div></div>@endforeach</div><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Ученик</th><th>Этап</th><th>Срок</th><th>Сумма</th><th>Оплачено</th><th>Остаток</th><th>Статус</th></tr></thead><tbody>@forelse($installments as $row)<tr><td>{{ $row->invoice->student?->full_name }}</td><td>{{ $row->name_ru }}</td><td>{{ $row->due_date->format('d.m.Y') }}</td><td>{{ $row->amount }} EGP</td><td>{{ $row->paid_amount }} EGP</td><td>{{ $row->remaining_amount }} EGP</td><td>{{ $row->statusLabel() }}</td></tr>@empty<tr><td colspan="7">Графики рассрочки отсутствуют.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
