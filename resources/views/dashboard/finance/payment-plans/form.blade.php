@extends('layouts.dashboard')
@section('content')@php $rows=old('installments',$plan->exists?$plan->installments->map->only(['name_ru','offset_days','percentage'])->all():[['name_ru'=>'Первый взнос','offset_days'=>0,'percentage'=>'100.0000']]); @endphp<div class="container py-4"><h1 class="h3">{{ $plan->exists?'Изменить план оплаты':'Новый план оплаты' }}</h1>@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ $plan->exists?route('dashboard.finance.payment-plans.update',$plan):route('dashboard.finance.payment-plans.store') }}" class="card card-body">@csrf @if($plan->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">Название</label><input name="name_ru" value="{{ old('name_ru',$plan->name_ru) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Порядок</label><input type="number" min="0" name="sort_order" value="{{ old('sort_order',$plan->sort_order??0) }}" class="form-control"></div><div class="col-md-3 form-check mt-5"><input type="hidden" name="is_active" value="0"><input type="checkbox" name="is_active" value="1" class="form-check-input" @checked(old('is_active',$plan->is_active??true))><label>Активен</label></div><div class="col-12"><label class="form-label">Описание</label><textarea name="description" class="form-control">{{ old('description',$plan->description) }}</textarea></div></div><hr><h2 class="h5">Этапы</h2><div id="segments">@foreach($rows as $i=>$row)<div class="row g-2 mb-2 segment"><div class="col-md-5"><input name="installments[{{ $i }}][name_ru]" value="{{ $row['name_ru'] }}" class="form-control" placeholder="Название этапа" required></div><div class="col-md-3"><input type="number" min="0" name="installments[{{ $i }}][offset_days]" value="{{ $row['offset_days'] }}" class="form-control" placeholder="Смещение, дней" required></div><div class="col-md-3"><input type="number" min="0.0001" step="0.0001" name="installments[{{ $i }}][percentage]" value="{{ $row['percentage'] }}" class="form-control" placeholder="Доля, %" required></div></div>@endforeach</div><p class="form-text">Доли должны составлять 100%. Суммы можно определить вручную при создании конкретного счёта.</p><button class="btn btn-primary mt-3">Сохранить</button></form></div>@endsection
