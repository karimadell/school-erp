@extends('layouts.dashboard')
@section('content')<div class="container-fluid py-4"><div class="d-flex justify-content-between"><div><h2>{{ $fee->name_ru }}</h2><span class="badge bg-{{ $fee->is_active?'success':'secondary' }}">{{ $fee->is_active?'Активен':'Неактивен' }}</span></div><div><a class="btn btn-outline-secondary" href="{{ route('dashboard.finance.services.edit',$fee) }}">Изменить услугу</a> <a class="btn btn-primary" href="{{ route('dashboard.finance.tariffs.create',['fee_id'=>$fee->id]) }}">Добавить новый тариф</a></div></div><p class="alert alert-info mt-3">Изменение цены создаёт новую версию тарифа и не меняет старые счета.</p>
<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Учебный год</th><th>Цена</th><th>Действует с</th><th>Действует до</th><th>Статус</th><th>Причина</th></tr></thead><tbody>@forelse($fee->prices as $p)<tr><td>{{ $p->academicYear?->name }}</td><td><a href="{{ route('dashboard.finance.tariffs.show',$p) }}">{{ number_format($p->amount,2,'.','') }} EGP</a></td><td>{{ $p->start_date->format('d.m.Y') }}</td><td>{{ $p->end_date?->format('d.m.Y') ?? 'Без ограничения' }}</td><td>{{ ['current'=>'Текущий','future'=>'Будущий','expired'=>'Завершён','inactive'=>'Неактивен'][$p->status()] }}</td><td>{{ $p->change_reason ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">Тарифы не созданы.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
