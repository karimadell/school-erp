<div class="row g-3">@forelse($files as $file)<div class="col-12"><div class="card border-0 shadow-sm"><div class="card-body"><div class="d-flex flex-wrap justify-content-between gap-3"><div><h2 class="h6">{{ $file->typeLabel() }}</h2><div>{{ $file->description ?: 'Без описания' }}</div><small class="text-muted">{{ $file->file_name }} · {{ number_format($file->file_size/1024,1) }} КБ · загружен {{ $file->created_at?->format('d.m.Y H:i') }}{{ $file->uploader ? ' · '.$file->uploader->name : '' }}</small>@if($file->issue_date)<div>Дата выдачи: {{ $file->issue_date->format('d.m.Y') }}</div>@endif @if($file->expiry_date)<div>Срок действия: {{ $file->expiry_date->format('d.m.Y') }} · <span class="badge {{ $file->expiryStatus()==='Просрочен'?'bg-danger':($file->expiryStatus()==='Скоро истекает'?'bg-warning text-dark':'bg-success') }}">{{ $file->expiryStatus() }}</span></div>@endif</div><div class="d-flex flex-wrap gap-2"><a class="btn btn-sm btn-outline-primary" target="_blank" href="{{ route('dashboard.students.documents.preview',[$file->student_id,$file]) }}">Просмотреть</a><a class="btn btn-sm btn-outline-secondary" href="{{ route('dashboard.students.documents.download',[$file->student_id,$file]) }}">Скачать</a>@if($archived)<form method="post" action="{{ route('dashboard.students.documents.restore',[$file->student_id,$file]) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-success">Восстановить</button></form>@else<form class="d-flex gap-1" method="post" action="{{ route('dashboard.students.documents.archive',[$file->student_id,$file]) }}" onsubmit="return confirm('Переместить документ в архив?')">@csrf @method('PATCH')<input class="form-control form-control-sm" name="archive_reason" placeholder="Причина" required><button class="btn btn-sm btn-outline-danger">Архивировать</button></form>@endif</div></div></div></div></div>@empty<div class="col-12"><div class="alert alert-light">Документы отсутствуют.</div></div>@endforelse</div>
