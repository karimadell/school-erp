@extends('layouts.dashboard')
@section('content')<div class="container py-4"><h1 class="h3">{{ $subscription->exists?'Изменить услугу':'Добавить услугу ученику' }}</h1><div class="text-muted mb-3">{{ $student->full_name }}</div>@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ $subscription->exists?route('dashboard.students.subscriptions.update',[$student,$subscription]):route('dashboard.students.subscriptions.store',$student) }}" class="card card-body">@csrf @if($subscription->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">Учебный год</label><select name="academic_year_id" class="form-select" required>@foreach($enrollments as $enrollment)<option value="{{ $enrollment->academic_year_id }}" @selected(old('academic_year_id',$subscription->enrollment?->academic_year_id)==$enrollment->academic_year_id)>{{ $enrollment->academicYear?->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Услуга</label><select name="fee_id" class="form-select" required>@foreach($fees as $fee)<option value="{{ $fee->id }}" @selected(old('fee_id',$subscription->fee_id)==$fee->id)>{{ $fee->name_ru }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Дата начала</label><input type="date" name="start_date" value="{{ old('start_date',$subscription->start_date?->format('Y-m-d')) }}" class="form-control" required></div><div class="col-md-4"><label class="form-label">Дата окончания</label><input type="date" name="end_date" value="{{ old('end_date',$subscription->end_date?->format('Y-m-d')) }}" class="form-control"></div><div class="col-md-4"><label class="form-label">Количество</label><input type="number" min="1" name="quantity" value="{{ old('quantity',$subscription->quantity?:1) }}" class="form-control" required></div><div class="col-12"><label class="form-label">Причина / примечание</label><textarea name="reason" class="form-control">{{ old('reason') }}</textarea></div></div><button class="btn btn-primary mt-3">Сохранить</button></form></div>@endsection
