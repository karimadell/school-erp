@extends('layouts.dashboard')
@section('content')<div class="container-fluid py-4"><div class="d-flex justify-content-between mb-3"><div><h1 class="h3">Услуги ученика</h1><div class="text-muted">{{ $student->full_name }}</div></div><div><a class="btn btn-outline-primary" href="{{ route('dashboard.students.subscriptions.renew',$student) }}">Продлить услуги</a> <a class="btn btn-primary" href="{{ route('dashboard.students.subscriptions.create',$student) }}">Добавить услугу</a></div></div><form class="row g-2 mb-3"><div class="col-md-3"><select name="academic_year_id" class="form-select"><option value="">Все учебные годы</option>@foreach($years as $year)<option value="{{ $year->id }}" @selected(request('academic_year_id')==$year->id)>{{ $year->name }}</option>@endforeach</select></div><div class="col-md-3"><select name="status" class="form-select"><option value="">Все статусы</option><option value="active">Активна</option><option value="suspended">Приостановлена</option><option value="completed">Завершена</option></select></div><button class="btn btn-outline-secondary col-md-2">Фильтр</button></form><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Услуга</th><th>Учебный год</th><th>Статус</th><th>Начало</th><th>Окончание</th><th>Вариант</th><th></th></tr></thead><tbody>@forelse($subscriptions as $subscription)<tr><td>{{ $subscription->fee?->name_ru }}</td><td>{{ $subscription->enrollment?->academicYear?->name }}</td><td>{{ $subscription->statusLabel() }}</td><td>{{ $subscription->start_date?->format('d.m.Y') }}</td><td>{{ $subscription->end_date?->format('d.m.Y') ?: '—' }}</td><td>{{ collect($subscription->metadata??[])->filter()->implode(' · ') ?: '—' }}</td><td><a href="{{ route('dashboard.students.subscriptions.show',[$student,$subscription]) }}">Открыть</a></td></tr>@empty<tr><td colspan="7">Услуги не подключены.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
