@extends('layouts.dashboard')
@section('content')<div class="container py-4"><h1 class="h3">Продление услуг на новый учебный год</h1><p class="text-muted">Существующие услуги и счета не изменяются. Будут созданы новые записи.</p><form method="POST" action="{{ route('dashboard.students.subscriptions.renew.store',$student) }}" class="card card-body">@csrf<div class="row g-3 mb-3"><div class="col-md-6"><label class="form-label">Исходный учебный год</label><select name="source_academic_year_id" class="form-select" required>@foreach($years as $year)<option value="{{ $year->id }}">{{ $year->name }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">Новый учебный год</label><select name="target_academic_year_id" class="form-select" required>@foreach($years as $year)<option value="{{ $year->id }}">{{ $year->name }}</option>@endforeach</select></div></div>@foreach($subscriptions as $subscription)<div class="form-check border rounded p-3 mb-2"><input class="form-check-input" type="checkbox" name="subscription_ids[]" value="{{ $subscription->id }}" id="sub{{ $subscription->id }}"><label class="form-check-label" for="sub{{ $subscription->id }}">{{ $subscription->fee?->name_ru }} · {{ $subscription->enrollment?->academicYear?->name }}</label></div>@endforeach<button class="btn btn-primary mt-3">Продлить выбранные услуги</button></form></div>@endsection
